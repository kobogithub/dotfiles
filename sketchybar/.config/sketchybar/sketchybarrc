#!/usr/bin/env bash
# Configuración de sketchybar — tema Frostmourne, pensada para AeroSpace.
#
# La altura de la barra debe coincidir con gaps.outer.top en
# aerospace/.aerospace.toml, o las ventanas quedan por debajo y la tapan.

export CONFIG_DIR="$HOME/.config/sketchybar"
export PLUGIN_DIR="$CONFIG_DIR/plugins"
export FONT="Iosevka Nerd Font"
export BAR_HEIGHT=32

source "$CONFIG_DIR/colors.sh"

##### Barra #####
sketchybar --bar \
    height="$BAR_HEIGHT" \
    position=top \
    sticky=on \
    topmost=off \
    shadow=off \
    padding_left=6 \
    padding_right=6 \
    color="$BAR_BG"

##### Valores por defecto de todo item #####
sketchybar --default \
    updates=when_shown \
    icon.font="$FONT:Bold:14.0" \
    icon.color="$FG" \
    icon.padding_left=8 \
    icon.padding_right=4 \
    label.font="$FONT:Semibold:13.0" \
    label.color="$FG" \
    label.padding_left=4 \
    label.padding_right=8 \
    background.color="$ITEM_BG" \
    background.corner_radius=6 \
    background.height=22 \
    background.drawing=off

##### Items #####
source "$CONFIG_DIR/items/spaces.sh"
source "$CONFIG_DIR/items/front_app.sh"
source "$CONFIG_DIR/items/clock.sh"
source "$CONFIG_DIR/items/battery.sh"
source "$CONFIG_DIR/items/volume.sh"
source "$CONFIG_DIR/items/network.sh"

##### Aplicar #####
sketchybar --update
